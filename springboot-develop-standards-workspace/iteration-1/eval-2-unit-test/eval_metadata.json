{
  "eval_id": 2,
  "eval_name": "unit-test-design",
  "prompt": "请为以下 Service 层代码设计单元测试用例：\n\n@Service\n@Transactional(rollbackFor = Exception.class)\npublic class OrderServiceImpl implements OrderService {\n\n    private final OrderRepository orderRepository;\n    private final ProductService productService;\n\n    public OrderServiceImpl(OrderRepository orderRepository, ProductService productService) {\n        this.orderRepository = orderRepository;\n        this.productService = productService;\n    }\n\n    @Override\n    public OrderVO createOrder(OrderDTO orderDTO) {\n        ProductVO product = productService.getProductById(orderDTO.getProductId());\n        if (product == null) {\n            throw new BusinessException(404, \"商品不存在\");\n        }\n        if (product.getStock() < orderDTO.getQuantity()) {\n            throw new BusinessException(400, \"库存不足\");\n        }\n        OrderEntity order = new OrderEntity();\n        order.setProductId(orderDTO.getProductId());\n        order.setQuantity(orderDTO.getQuantity());\n        order.setTotalPrice(product.getPrice() * orderDTO.getQuantity());\n        order.setStatus(1);\n        orderRepository.save(order);\n        return toVO(order);\n    }\n}",
  "assertions": [
    "提供 createOrder_Success 测试用例，包含 Given-When-Then 结构",
    "提供 createOrder_ProductNotFound 异常场景测试",
    "提供 createOrder_StockNotEnough 库存不足场景测试",
    "使用 @Mock 模拟依赖对象"
  ]
}
